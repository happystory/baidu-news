doctype
html
    head
        meta(charset='utf-8')
        title 404
    body
        h1= message
        h2= error.status
        mixin link(page, pathname)
            p 
                span #{page}：
                a(href='/#{pathname}', target='_blank') /#{pathname}
        +link('首页','')
        +link('详情页', 'news/14')
        +link('后台管理页', 'admin')
        +link('后台添加页', 'admin/create')
        +link('后台更新页', 'admin/update/14')

